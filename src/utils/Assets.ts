const publicUrl = process.env.PUBLIC_URL;
const imgBase = `${publicUrl}/img`;

const uaFlagPath = `${imgBase}/ua_flag.png`
const usFlagPath = `${imgBase}/us_flag.png`

export {
    uaFlagPath,
    usFlagPath
}